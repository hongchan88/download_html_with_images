{
  "name": "download_html_with_images",
  "version": "1.0.0",
  "description": "download html files with images include. a tag and img src will be changed to local path so it will work as offline as well.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.2",
    "cheerio": "^1.0.0-rc.12"
  }
}
